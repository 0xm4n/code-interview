题目：

题目分析：https://xin-tan.com/passages/2019-06-23-tree-tail-order/

代码：
function VerifySquenceOfBST(sequence) {
  if (!sequence.length) return false;
  return isBST(sequence);
}

function isBST(sequence) {
  if (sequence.length === 0) {
    return true;
  }
  const length = sequence.length;
  let root = sequence[length - 1],
    i,
    j;
  for (i = 0; i < length - 1 && sequence[i] < root; i++);
  for (j = i; j < length - 1 && sequence[j] > root; j++);

  if (j !== length - 1) {
    return false;
  }

  let left = isBST(sequence.slice(0, i));
  let right = isBST(sequence.slice(i, length - 1));

  return left && right;
}